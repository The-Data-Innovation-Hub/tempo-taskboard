// This file is intentionally empty as all email functionality has been removed

// Export placeholder functions to prevent import errors
export const sendProjectInvitation = async () => {
  console.warn("Email functionality has been removed");
  return { success: true };
};

export const sendWelcomeEmail = async () => {
  console.warn("Email functionality has been removed");
  return { success: true };
};
